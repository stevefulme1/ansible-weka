---
# Integration tests for stevefulme1.weka.weka_s3_bucket

- name: List weka_s3_bucket resources
  stevefulme1.weka.weka_s3_bucket_info:
    host: "{{ weka_host | default('localhost') }}"
    username: "{{ weka_username | default('admin') }}"
    password: "{{ weka_password | default('password') }}"
    validate_certs: "{{ weka_validate_certs | default(false) }}"
  register: list_result

- name: Verify weka_s3_bucket listing
  ansible.builtin.assert:
    that:
      - list_result is not failed

- name: Create weka_s3_bucket (check_mode)
  stevefulme1.weka.weka_s3_bucket:
    host: "{{ weka_host | default('localhost') }}"
    username: "{{ weka_username | default('admin') }}"
    password: "{{ weka_password | default('password') }}"
    validate_certs: "{{ weka_validate_certs | default(false) }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: present
  check_mode: true
  register: check_result

- name: Verify check_mode reports changed
  ansible.builtin.assert:
    that:
      - check_result is changed

- name: Create weka_s3_bucket
  stevefulme1.weka.weka_s3_bucket:
    host: "{{ weka_host | default('localhost') }}"
    username: "{{ weka_username | default('admin') }}"
    password: "{{ weka_password | default('password') }}"
    validate_certs: "{{ weka_validate_certs | default(false) }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: present
  register: create_result

- name: Verify weka_s3_bucket was created
  ansible.builtin.assert:
    that:
      - create_result is changed

- name: Create weka_s3_bucket again (idempotency)
  stevefulme1.weka.weka_s3_bucket:
    host: "{{ weka_host | default('localhost') }}"
    username: "{{ weka_username | default('admin') }}"
    password: "{{ weka_password | default('password') }}"
    validate_certs: "{{ weka_validate_certs | default(false) }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: present
  register: idem_result

- name: Verify idempotent create
  ansible.builtin.assert:
    that:
      - idem_result is not changed

- name: Delete weka_s3_bucket (check_mode)
  stevefulme1.weka.weka_s3_bucket:
    host: "{{ weka_host | default('localhost') }}"
    username: "{{ weka_username | default('admin') }}"
    password: "{{ weka_password | default('password') }}"
    validate_certs: "{{ weka_validate_certs | default(false) }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: absent
  check_mode: true
  register: delete_check

- name: Verify check_mode delete reports changed
  ansible.builtin.assert:
    that:
      - delete_check is changed

- name: Delete weka_s3_bucket
  stevefulme1.weka.weka_s3_bucket:
    host: "{{ weka_host | default('localhost') }}"
    username: "{{ weka_username | default('admin') }}"
    password: "{{ weka_password | default('password') }}"
    validate_certs: "{{ weka_validate_certs | default(false) }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: absent
  register: delete_result

- name: Verify weka_s3_bucket was deleted
  ansible.builtin.assert:
    that:
      - delete_result is changed

- name: Delete weka_s3_bucket again (idempotent)
  stevefulme1.weka.weka_s3_bucket:
    host: "{{ weka_host | default('localhost') }}"
    username: "{{ weka_username | default('admin') }}"
    password: "{{ weka_password | default('password') }}"
    validate_certs: "{{ weka_validate_certs | default(false) }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: absent
  register: delete_idem

- name: Verify idempotent delete
  ansible.builtin.assert:
    that:
      - delete_idem is not changed
