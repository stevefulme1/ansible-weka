---
argument_specs:
  main:
    short_description: "Configure snapshots and restore procedures"
    options:
      weka_backup_restore_host:
        description: Target host address
        type: str
        required: true
      weka_backup_restore_username:
        description: Authentication username
        type: str
      weka_backup_restore_password:
        description: Authentication password
        type: str
      weka_backup_restore_api_key:
        description: API key for authentication
        type: str
      weka_backup_restore_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      weka_backup_restore_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
