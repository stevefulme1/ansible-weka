---
argument_specs:
  main:
    short_description: "Rolling cluster upgrade"
    options:
      weka_upgrade_host:
        description: Target host address
        type: str
        required: true
      weka_upgrade_username:
        description: Authentication username
        type: str
      weka_upgrade_password:
        description: Authentication password
        type: str
        no_log: true
      weka_upgrade_api_key:
        description: API key for authentication
        type: str
        no_log: true
      weka_upgrade_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      weka_upgrade_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
