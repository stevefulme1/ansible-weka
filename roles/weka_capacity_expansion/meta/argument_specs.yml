---
argument_specs:
  main:
    short_description: "Add drives and nodes to existing cluster"
    options:
      weka_capacity_expansion_host:
        description: Target host address
        type: str
        required: true
      weka_capacity_expansion_username:
        description: Authentication username
        type: str
      weka_capacity_expansion_password:
        description: Authentication password
        type: str
        no_log: true
      weka_capacity_expansion_api_key:
        description: API key for authentication
        type: str
        no_log: true
      weka_capacity_expansion_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      weka_capacity_expansion_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
